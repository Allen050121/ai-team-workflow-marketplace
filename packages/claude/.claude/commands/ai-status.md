# AI Team Status

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .ai-team/scripts/Get-AiTeamStatus.ps1
```

Use the output to choose the next role: Dispatcher, Executor, Reviewer, Integration, or Memory Curator.
