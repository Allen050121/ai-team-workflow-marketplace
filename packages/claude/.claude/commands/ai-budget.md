# AI Team Context Budget

Measure context size before loading large memory or run evidence:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .ai-team/scripts/Measure-AiTeamContext.ps1
```

Compress memory files and archive noisy run evidence when budget warnings appear.
