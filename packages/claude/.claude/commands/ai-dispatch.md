# AI Team Dispatch

Act as Dispatcher.

Run or inspect:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .ai-team/scripts/Get-AiTeamIntake.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .ai-team/scripts/Get-AiTeamContext.ps1 -Mode compact
```

Create the fewest useful task cards. Set `work_mode`, `workflow_mode`, dependencies, allowed files, acceptance criteria, and verification commands.
